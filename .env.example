# Firebase Configuration
# For production (Render), use GOOGLE_APPLICATION_CREDENTIALS_JSON
# For local development, use serviceAccountKey.json file
GOOGLE_APPLICATION_CREDENTIALS_JSON=

# Server Configuration
PORT=8080

# FastAPI ML Model Endpoint
FASTAPI_URL=https://mukthish-automeet.hf.space/predict

# Email Configuration
# Option 1 (Recommended for Production): SendGrid API
# Sign up at https://signup.sendgrid.com/ (free tier: 100 emails/day)
# Get API key from: Settings > API Keys > Create API Key
# Verify sender email at: Settings > Sender Authentication
SENDGRID_API_KEY=

# Option 2 (Local Development Fallback): SMTP
# For Gmail: Use App Password (https://support.google.com/accounts/answer/185833)
# For other providers: Check their SMTP settings
# Note: SMTP may not work on cloud platforms like Render (port blocking)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
